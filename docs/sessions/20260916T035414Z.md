# Session 20260916T035414Z
Task: S0 + S1 (first session, project created from zero)
Start commit: none (empty directory containing only the two spec documents)
End commit: see `git log` after this session's final commit

## Changed files
New repository. README.md, AGENTS.md, CLAUDE.md, .gitignore,
docs/{STATE,ROADMAP,ENVIRONMENT,DECISIONS}.md, docs/tasks/{S1,S2}.md,
docs/sessions/20260916T035414Z.md, config/isaac_env.json, profiles/s1_smoke_v1.json,
scripts/pf, src/parcel_forge/{__init__,envprobe,doctor,evidence,pngio,smoke_s1,smoke_host}.py,
src/parcel_forge/runtime/{__init__,launcher,isaacsim_runtime}.py,
tests/{test_pngio,test_free_fall_reference}.py, runs/.gitkeep.

## What changed and why
S0: read-only inventory of the machine, a doctor that refuses to guess, and the
handoff documents. S1: a runtime adapter for the installed Isaac Sim 6.0.1.0 plus a
finite-step cube-drop smoke that saves its own evidence. Nothing was installed,
upgraded or stopped on this machine.

## Checks run
| command | exit | evidence |
| --- | --- | --- |
| `./scripts/pf doctor` | 0 (overall=warn) | runs/<doctor run>/doctor.json |
| `./scripts/pf smoke` (first attempt) | 4 | runs/20260916T034844Z_s1_smoke/ |
| `./scripts/pf smoke` (after fix) | 0 | runs/20260916T035235Z_s1_smoke/ |
| `python3 -m unittest discover -s tests` | 0 | 9 tests OK |

## Passed
Physics execution, per-step state read-back, headless operation, offline PNG render
of the real final scene, free-fall agreement with theory to 2.04 mm, cube resting at
exactly half its edge height. Numbers in docs/STATE.md and the run's s1_result.json.

## Failed / blocked
- First smoke attempt exited 4 because `SimulationApp.close()` terminated the process
  before `s1_result.json` was written. Physics and the PNG had actually succeeded.
  Fixed; the failed run is kept.
- WebRTC viewing of a parcel-forge scene remains not_tested by design.

## Decisions
D001 standalone launcher (not Isaac Lab), D002 Isaac Sim 6.0 experimental core API,
D003 livestream always off, D004 stdlib PNG encoder, D005 split ground collision/visual.

## Note for the user
During the first smoke run the existing WebRTC viewer process changed PID: the old
process disappeared and a new `view_usd_webrtc.py` (PID 2430488) started at
11:49:48 local and reports `READY - streaming`. parcel-forge ran no start/stop script
and signalled no process; the cause is unknown. Please confirm your viewer is healthy.

## Learning
A discrete integrator does not equal the textbook formula; the S1 tolerance exists to
hold that gap, and the measured 2.04 mm matches the predicted 0.5*g*dt*t exactly.

## Next session
Write `src/parcel_forge/geometry.py` plus its unit test and verify the five-plate
table gives Li=0.29, Wi=0.19, Hi=0.145 m before launching the simulator (docs/tasks/S2.md).

## Background process
None left running by parcel-forge. All launched Isaac processes exited.
