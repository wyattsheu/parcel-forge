# 單一紙箱裝鍵盤實測

使用者 steering：只測一個，不跑其他複合模型。
./scripts/pf-keyboard-package 第一 run 235543Z exit 1，board_mass import 錯誤；修復函式來源，第二 run 235608Z exit 0。
證據：runs/20260917T235608Z_keyboard_package。
四蓋外力開啟／有限讀回／鍵盤留箱 checks true；mass 0.65 kg。固定箱底、剛性 keyboard proxy；extract/render/human WebRTC/calibration not_tested。
python3 -m py_compile keyboard_package.py keyboard_package_editor.py exit 0；loader 未 GUI 驗證。
報告含唯一主案例重跑命令與 Script Editor 入口。保留所有原始失敗，沒有更動共享環境或 viewer。
下一步：同一鍵盤包裹取出／接觸與自由箱體驗證。
