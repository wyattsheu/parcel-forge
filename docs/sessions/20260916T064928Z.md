# Session 20260916T064928Z
Task: S3 (parameterisation and static validation)
Start commit: 4737f08
End commit: see `git log` after this session's final commit

## Changed files
New: src/parcel_forge/{schema,usd_author,usd_tool,build_host}.py,
src/parcel_forge/validation/static_usd.py, profiles/static_usd_v1.json,
cases/{open_box_small,open_box_tall,open_box_thick_wall}.json,
cases/invalid/ (7 specs + expected_errors.json), tests/test_schema.py, docs/tasks/S4.md.
Changed: scripts/pf (build/verify replace their exit-4 stubs), README.md,
docs/{STATE,ROADMAP,DECISIONS}.md, docs/tasks/S3.md.

## What changed and why
A case file now drives the whole pipeline. The schema runs first on the plain system
Python and rejects illegal specifications with named error classes; only then is a
USD asset authored, and the static checks re-read that written file rather than the
input. Three more legal box sizes were added so parameterisation is actually exercised.

## Checks run
| command | exit | evidence |
| --- | --- | --- |
| `./scripts/pf verify --case open_box_normal` | 0 | runs/20260916T064557Z_s3_verify_open_box_normal/ |
| `./scripts/pf verify --all` | 0 (4.2 s) | 13 runs + runs/20260916T064613Z_s3_verify_bad_wall_too_thick_suite.md |
| `./scripts/pf box --all` | 0 | 6 runs + runs/20260916T064747Z_s2_open_box_thick_wall_suite.md |
| `python3 -m unittest discover -s tests` | 0 | 57 tests OK |

## Passed
All 6 legal cases built, statically validated and simulated to their expected outcome.
All 7 invalid cases rejected for their declared reason with no simulator launch.
Largest static dimension error 1.2e-8 m against a 1e-4 m tolerance.
The thick-wall case landed at box-local z = 0.040000 m, matching the prediction
t + probe/2 = 0.02 + 0.02 exactly (this was last session's exercise, now a permanent case).

## Failed / blocked
- Isaac Sim's official asset-validation rule set is still not invoked; reported as
  `blocked` in every validation.json, never as a pass.
- `pf validate` is reachable only through `pf verify`; a standalone
  `--asset` entry point was not built.

## Decisions
D008 build/validate run on pxr alone without Kit (4.2 s for 13 cases, no GPU);
D009 invalid case files live in cases/invalid/ with a sidecar expectation file.

## Learning
The cheapest check should run first. Schema validation costs milliseconds on the
system Python, USD authoring costs seconds, and PhysX costs minutes and a GPU. An
illegal spec now dies in the first stage, so a typo never burns a simulator launch.

## Next session
Write `src/parcel_forge/mass_properties.py` and its unit test against the handbook
section 19 table, before touching the simulator (docs/tasks/S4.md).

## Background process
None left running by parcel-forge. All launched Isaac processes exited.
