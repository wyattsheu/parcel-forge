# 匯出包與 USD 物理邊界

使用者：joint drag 只能抓整箱、沒用，刪掉；四個施力欄位也刪掉；紙箱已很完整但敲不會凹壞；
先存暫存；怎麼輸出給別人、物理性質是否在 USD 裡。

改動：carton_live 移除 joint 模式與按鈕、移除 ForceProbe UI（類別保留）；
新增 src/parcel_forge/carton_export_check.py 與 scripts/pf-carton-export；
carton_usd_check 限位改讀 config。

過程中的失敗與修正（皆保留證據）：
- 151756Z：PhysxSchema 在未啟動 Kit 時無法 import → 改用屬性名讀取
- 151825Z：gains_match_config=false → 讀回迴圈變數遮蔽蓋子名稱，修正後重跑
- README 原寫 0.085N（僅摺痕）→ 改用含自重的 0.14N，與先前回報一致

命令與結果：
- `./scripts/pf-carton-export --run 20260917T150447Z_ext1_carton_pull --name carton_v1` → exit 0，
  runs/20260917T151929Z_ext1_carton_export，4 joints、勁度與 config 全符
- `./scripts/pf-carton-editor-test` → exit 0
- `python3 -m unittest discover -s tests` → 160 tests、7 skipped、exit 0
- `./scripts/pf smoke` → exit 0，runs/20260917T151955Z_s1_smoke

未執行 git commit（大量既存未追蹤檔案，待使用者決定）。
未驗證：板面變形（未模擬）、他人機器上載入 load_in_isaacsim.py、真實紙板校正。
下一個精確動作：使用者決定是否 commit；之後選定板面凹陷／破壞的方法。
文件：D068、STATE、EXT1 卡、reports/development/2026-09-17_export_package.md。
