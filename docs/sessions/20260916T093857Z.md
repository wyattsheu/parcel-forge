# Session 20260916T093857Z
Task: methods review against the handbook's section 3 sources
Start commit: 080b8cc
End commit: see `git log`

## Why
Challenged on whether the cited sources were actually used. Audit result: the
principles in the "本計畫採用什麼" column were implemented (workflow/tool separation,
per-run directories, digest binding, provenance tiers, version-matched API reading),
but the sources themselves had never been opened, and two gaps were real:
typed tools, and an unused validation entry point.

## Changed files
New: src/parcel_forge/validation/official_usd.py, docs/METHODS.md, docs/tasks/S5.md.
Changed: src/parcel_forge/usd_tool.py, src/parcel_forge/validation/static_usd.py,
docs/{STATE,DECISIONS}.md, docs/tasks/S4.md, README.md.

## What changed and why
Read the usd-content-agents repo and its agentic README, plus the
Articulate-Anything, LL3M and Scalable Real2Sim papers. Found that NVIDIA's own
asset validator is installed locally and imports without Kit, and wired it in.
Encoded the papers' reported failure modes as binding design rules rather than
inspiration.

## Checks run
| command | exit | evidence |
| --- | --- | --- |
| official validator against our asset | - | 41 rules, 0 issues |
| official validator against a broken fixture | - | 3 FAILUREs, so the engine can fail |
| `./scripts/pf verify --case open_box_normal` | 0 | runs/20260916T093523Z_s3_verify_open_box_normal/ |
| `./scripts/pf verify --all` | 0 (4.9 s) | runs/20260916T093536Z_s3_verify_bad_wall_too_thick_suite.md |

## Passed
G1.official_isaac_asset_validation moved from `blocked` to `pass` on real evidence.
G7.official_validator_can_fail added and passing. All 13 cases still correct.

## Failed / blocked
Still not implemented: typed tool declarations, workflow_state.json (resume),
decisions/ and traces/ directories, and the version-matched API lookup tool (D011).

## Decisions
D010 official validator plus a reliability self-test; D011 API lookup as a tool;
D012 a model-based critic may never upgrade a verdict.

## Learning
The most useful thing in the Articulate-Anything paper is not its architecture, it is
its reported failure: the critic's dominant error is calling a wrong result correct.
That single sentence is why the model never gets to say "pass" in this project.

## Next session
S4: write `src/parcel_forge/mass_properties.py` with the closed-form tensor, the
confidence tiers, and the pseudo-inertia feasibility check, tested against the
handbook section 19 table (docs/tasks/S4.md).

## Background process
None left running by parcel-forge.
