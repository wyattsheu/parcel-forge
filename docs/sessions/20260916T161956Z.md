# Session: S4 dynamic readback and coverage

UTC date: 2026-09-16

## Commands and exit codes

- ./scripts/pf smoke — exit 0; required baseline in
  runs/20260916T152014Z_s1_smoke/.
- python3 -m unittest tests.test_mass_properties — exit 0; 32 tests after
  principal-axis decomposition work.
- python3 -m unittest discover -s tests -v — exit 0; final recorded regression
  has 101 tests, 7 skipped in runs/20260916T161109Z_s4_offline_regression/.
- ./scripts/pf verify --case open_box_dynamic — exit 0;
  runs/20260916T155155Z_s3_verify_open_box_dynamic/.
- ./scripts/pf mass-readback --timeout 1800 — exit 0;
  runs/20260916T155652Z_s4_mass_readback/.
- ./scripts/pf placement-grid — first attempt exit 3 because Isaac 6 path patterns
  require regular expression dot-star, not glob star. Preserved in
  runs/20260916T155958Z_s4_placement_grid/.
- ./scripts/pf placement-grid after fix — exit 0, 9/9 inside, PNG nonblank;
  runs/20260916T160040Z_s4_placement_grid/.
- ./scripts/pf sidewall — parcel-forge exit 4, external exit -11 during optional
  render. The run already contained 240 trajectory samples for each of four shots
  and scene_final.usda: runs/20260916T160621Z_s4_sidewall/.
- Offline immutable-trajectory analysis — exit 0, 4/4 blocked;
  runs/20260916T161010Z_s4_sidewall_recovery/.
- Report source/command audit — pass; all eight stage sections contain source and
  self-check coverage: runs/20260916T161908Z_report_source_commands/.
- git diff --check — exit 0 after removing trailing whitespace.

## Verified results

- Dynamic USD carries one root rigid body, five child colliders, explicit mass,
  local COM, principal inertia and principal axes.
- PhysX tensor-view transport errors: mass 2.98e-9 kg, COM 4.49e-11 m, inertia
  2.24e-11 kg m^2, principal axes 0.
- Nine requested placements all finished inside.
- Four side-wall shots reached their respective walls and did not exceed the
  declared geometric envelope plus 2 mm tolerance.
- Physics and optional rendering are now checkpointed and reported separately.
- Main development report now labels external methods versus project design and
  supplies copy-paste terminal/WebRTC commands for every applicable stage.

## Blocked / not tested

- Side-wall offline render failed in the source run; WebRTC human view is not_tested.
- Dynamic-box settling is not tested.
- Effective contact/rest offsets, friction, restitution and effective CCD state
  remain incomplete. GPU logs warn that suppress readback disables CCD despite the
  scene attribute reading true.

## Exact next action

Implement a contact-settings manifest that explicitly authors and reads back contact
and rest offsets, static/dynamic friction and restitution, and reports effective CCD
state separately from its USD attribute. Verify it in one fresh run before dynamic
box settling.
