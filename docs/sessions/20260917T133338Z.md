# 滑鼠拖曳力不足：力臂與 pickingForce

使用者回報滑鼠左鍵只能讓蓋子微動，並問是否摺痕太硬。讀 AGENTS/STATE/最新 session 與現有證據後查證，
先讀本機安裝的 omni.physx 110.1.13（版本相符的 API 參考），未上網猜測。

查到：拖曳力縮放設定為 /physics/pickingForce（使用者 app 讀回 1.0），官方 KaplaArenaDemo 設為 10；
安裝包未定義每單位牛頓數，且無可程式 grab API，故滑鼠實際送出的力無法 headless 量測，維持 not_tested。

可量的部分：資產需要的力。關著主蓋外緣 0.141N，與 ramp 實測 0.16N 一致；中點 2 倍、1/4 處 4 倍。
夾擊驗證 MinorXN 中點：0.7 倍 →1.6°，1.3 倍 →49.7°。

改動：carton_feel 新增 grab_force_table；ForceProbe.apply 新增 grab fraction；
LiveCarton 新增 grab_strength(v)（讀寫 /physics/pickingForce，印出原值還原）與 mouse_mode('joint'/'force')，
UI 新增抓取強度滑桿與需要力量標示，載入預設改 joint drag；carton_pull_test 新增中點夾擊與 pickingForce 讀寫還原檢查；
編輯器入口改指新 run 並印出三個做法。未改摺痕參數、未放寬門檻、未安裝／升級／重啟任何東西。

命令與結果：
- `./scripts/pf-carton-pull` → exit 0，runs/20260917T133338Z_ext1_carton_pull，18 checks 全 true
- `./scripts/pf-carton-editor-test` → exit 0，runs/20260917T133412Z_ext1_editor_loader
- `python3 -m unittest discover -s tests` → 160 tests、7 skipped、exit 0
- `./scripts/pf smoke` → exit 0，runs/20260917T133429Z_s1_smoke

未驗證：原生滑鼠拖曳（只有使用者能確認）、0.141N 對真實紙箱是否正確、render／影片／人眼。
下一個精確動作：使用者試抓外緣／joint drag／grab_strength(20) 後回報是哪一種模式、抓在哪、強度多少。
文件：D062、STATE、EXT1 卡、reports/development/2026-09-17_mouse_drag_force.md。
