# Session: S5-A pinned upstream baseline

User asked to start adopted plan and save playable test-process videos once the
workflow reaches a demonstrable stage. Current bounded task S5-A completed.

Read STATE, ROADMAP, ENVIRONMENT, S5-A, latest session and git status. Preserved
pre-existing changes. Read pinned upstream AGENTS and fixed-pipeline skill plus
validation-agent-cli reference. Explicit fixed-pipeline CLI selection matches plan.

## Commands, exits and evidence

- git clone --depth 1 upstream into external/usd-content-agents: exit 0.
- git checkout --detach a96faf9cb2f5c1f655fe0d60c0ccf57e3477b1aa: exit 0;
  upstream 0.6.0 clean source verified.
- uv venv --python /usr/bin/python3 .venvs/usd-content-agents: exit 0.
- uv pip install --python <isolated-python> -e <upstream> -e <validation_agent>:
  exit 0. No packages added to Isaac runtime; cache confined to /tmp.
- validation-agent run --help, uv pip freeze, uv pip check: exit 0.
- Original steel_scaffold_behavior_refine_summary.yaml with fresh output override:
  exit 0; verdict pass, no blocking issues. Fixture evidence only, no simulator.
- Missing required behavior summary config: exit 1; verdict fail,
  physics.behavior_evidence_missing. This expected rejection is not asset pass.
- ./scripts/pf smoke --physics-only: exit 0;
  runs/20260917T010648Z_s1_smoke/. Physics/readback pass, render not_tested.
- Report command shell syntax and git diff --check: exit 0.

Setup command arguments, output, fixture snapshots, source files, frozen package
versions, original upstream request/plan/result and checks saved in:
/mnt/HDD4/wyattsheu/ITRI/parcel-forge/runs/20260917T010638Z_s5a_upstream_setup/.

## Deliverables and limits

config/upstream_content_agents.json fixes full SHA; config/upstream_requirements.freeze.txt
records actual dependency versions. external/ and .venvs/ ignored by root Git;
reproducibility source/NOTICE snapshots and hashes retained in evidence.
Reports/development and execution updated. S5-A marked verified; S5-B card created.
No ITRI bridge, BYOR, live VLM, upstream rendering or new WebRTC viewer verified.
No test video produced at this evidence-only stage. Existing ffmpeg 6.1.1 detected;
S5-C now requires MP4 from exact recorded rollout with frames and encoder provenance,
plus separate human-view confirmation. No shared process was stopped or environment
reconfigured. No commit or push.

## Exact next action

Inspect pinned upstream physical_behavior evidence contract and implement minimal
normal/missing-bottom task mapping without treating regression pass as asset pass,
as scoped by docs/tasks/S5B_upstream_evidence.md.
