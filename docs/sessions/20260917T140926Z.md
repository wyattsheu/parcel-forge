# 推關保持與推/拖曳不對稱

使用者回報滑鼠推有效、拖曳無效，且推到關閉後會彈開，並引用紙箱回彈性資料。
推有效即證明其 viewer 的 omni.physx.ui 已啟用，D063 第一道閘門在使用者端通過。
讀安裝：/physics/mousePush 預設 1000 對 /physics/pickingForce 預設 1.0；
且 on_mouse_shift_drag_start 在 get_active_gesture()/get_active_hover() 有值時 return，點擊不受此限。

回彈量測（新增 press 階段）：第一次量壞並保留——四片全開時被推的主蓋走 11° 卡在 85.5°，
是撞到立起的次蓋，非回彈，兩項 fail 屬測試設計問題。重排為其他蓋關閉時做推測試，
並在次蓋階段前重新打開主蓋。結果：摺開 96.9° → −1N 推回 −0.34° → 放手仍 −0.34°，
回彈 0.00002°，塑性參考角 2.08°。推關就關著。

舊資產 segmented_directional_strip_v1 回彈 43.0°，新 rigid_panel_v1 4.0°；43° 正符合使用者描述，
因此加上載入時的資產識別列與 >15° WARNING，並把入口指向 20260917T140926Z_ext1_carton_pull。

命令與結果：
- `./scripts/pf-carton-pull` → exit 0，runs/20260917T140926Z_ext1_carton_pull，23 checks 全 true
- `./scripts/pf-carton-editor-test` → exit 0，runs/20260917T141003Z_ext1_editor_loader
- `python3 -m unittest discover -s tests` → 160 tests、7 skipped、exit 0
- `./scripts/pf smoke` → exit 0，runs/20260917T141032Z_s1_smoke

未驗證：原生滑鼠拖曳、真實紙板校正。未啟用擴充、未安裝、未重啟、未放寬門檻。
下一個精確動作：使用者重新載入入口並回報「Loaded asset」那一行與 blockers。
文件：D064、STATE、EXT1 卡、reports/development/2026-09-17_press_and_push.md。
