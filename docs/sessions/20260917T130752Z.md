# settle 量測修正與 legacy mixed-load gate 退役

讀 AGENTS/STATE/ROADMAP/ENVIRONMENT/EXT1/最新 session 與 git status，保留既有 dirty tree，未丟棄他人變更。
基線：`./scripts/pf smoke` exit0，runs/20260917T130254Z_s1_smoke。

診斷（先於修改）：105836Z 高力矩主蓋角度 7.2 秒 bit 不變於 0.2819564938545227 rad，DOF 速度讀回固定 0.2651888430118561 rad/s；
同 0.08 N·m 在 legacy-mixed 只讓次蓋動 0.57°，在 opening-order（主蓋先開）為 100°。使用者看過證據後授權兩項修正。

改動：src/parcel_forge/validation/carton.py（settle 以最後 1 秒角度峰對峰值判定，容差 0.02 rad/s 不變，速度讀回仍記錄；
legacy-mixed status 改為終端值 superseded，checks 照算照報）、scripts/pf-carton 預設情境改 opening-order、
新增 tests/test_carton_validation.py（9 項）。未動碰撞、材料參數、資產幾何、其他情境門檻；未裝套件；未動 Isaac 安裝。

命令與結果：
- `python3 -m unittest discover -s tests` → 145 tests、7 skipped、exit 0
- `./scripts/pf-carton --scenario opening-order` → exit 0，runs/20260917T130538Z_ext1_carton，opening_diagnostic_status pass
- `./scripts/pf-carton --scenario crease-coupon` → exit 0，runs/20260917T130614Z_ext1_carton，coupon pass
- `./scripts/pf-carton --scenario crease-cyclic` → exit 0，runs/20260917T130641Z_ext1_carton，coupon+cyclic pass
- `./scripts/pf-carton --scenario legacy-mixed` → exit 1，runs/20260917T130717Z_ext1_carton，status superseded，次蓋阻擋 fail 仍在 checks
- 離線重放 runs/20260917T130752Z_ext1_settle_spec_delivery/stale_velocity_replay.json：舊 gate settled=false，新 gate settled=true，該關節最後 1 秒角度變化 0.0 rad

130538Z 獨立重現速度讀回假象（HingeMinorXN 0.339 rad/s 對 0.0021 rad）。舊 run 目錄全部未更動。
文件：D060、STATE、EXT1 卡、reports/development/2026-09-17_settle_metric_and_legacy_gate.md。

未完成／未驗證：真實紙板校正 not_tested，所有材料參數 uncalibrated_assumption；板面僅一維條，箱壁與底剛體；
本輪無 render、影片、WebRTC 人眼；viewer 原生滑鼠與 UI 啟用例外狀態不變；S5D 未完成。
下一個精確動作：依 docs/research/CARTON_CALIBRATION_PROTOCOL.md 量一片真實主蓋 force-angle 曲線做參數辨識。
