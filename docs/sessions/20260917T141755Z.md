# resident stage 警告：升級回歸測試後仍無法重現

使用者貼出 UsdStage reference count 2 警告截圖，路徑指向前一個資產，即換資產瞬間。

先確認既有證據：三個既有 editor loader run 的 runtime.log 中該字串出現次數皆為 0，
但該測試第二次載入的是同一個資產，抓不到「關 A 開 B」。因此升級測試：
第三次載入改指不同資產，並在測試內掃描 out/runtime.log 找該字串，新增兩項 checks。

結果：警告未出現，stage_swap_completed 與 no_resident_stage_on_swap 皆 true。
對照實驗：暫時移除新加的 gc.collect() 後重跑，同樣沒有警告（原檔已從 scratchpad 還原）。
故 gc.collect() 為未經證實的預防措施，已在註解中如此標註，不宣稱為修正。

剩餘差異屬 GUI 專有（selection 與 property window 持有 prim），私有 Kit 無此視窗。
入口新增開新 stage 前清除自身選取，兼解 D064 的 gizmo 吞掉拖曳手勢問題；標為合理推測。

命令與結果：
- `./scripts/pf-carton-editor-test` → exit 0（含新增兩項 swap checks）
- 對照組（移除 gc.collect()）→ exit 0，同樣無警告
- `python3 -m unittest discover -s tests` → 160 tests、7 skipped、exit 0
- `./scripts/pf smoke` → exit 0，runs/20260917T141755Z_s1_smoke

未驗證：使用者 GUI 的該警告成因、原生滑鼠拖曳、真實紙板校正。
下一個精確動作：使用者重新載入入口，回報「Loaded asset」那一行、blockers，以及警告出現在哪一步。
文件：D065、STATE、EXT1 卡。
