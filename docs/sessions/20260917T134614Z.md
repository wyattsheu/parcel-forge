# 滑鼠拖曳：四道閘門與 blocked 的原生抓取測試

使用者回報 grab_strength 調到 1000 仍拉不動，並附 UsdStage refcount 警告截圖。
pickingForce 無效即排除力量大小，改查互動路徑本身；依 AGENTS 讀本機版本相符的安裝作為 API 參考。

查到（omni.physx.ui 110.1.13，physxViewportOverlays.on_mouse_shift_drag_start）：
四道閘門依序為擴充在跑且持有 overlay、timeline 播放中、整段按住 Shift（或 state=ENABLED）、
無其他 gesture/hover 佔用游標；通過後才呼叫 get_physx_interface().update_interaction。

實驗：程式化驅動該 API，並加入地面上的普通剛體對照方塊。結果無 POINT_GRABBED、對照方塊位移 2e-8 m，
即使 mouseInteractionEnabled/mouseGrab/forceGrab 全開。原因為 standalone app 未啟用 omni.physx.ui。
依 AGENTS 對缺資源的規定，該 run 記 status=blocked 並寫 blocked.json，host exit 4，不宣稱使用者 viewer 行為。
保留未歸因觀察：pickingForce 1000 時 MajorYP 瞬間 100° 但無 grab 事件。
過程中兩個自己造成的無效測試已修正並記錄：step() 不 pump 事件流、以及未設互動設定與缺地面。

改動：carton_mouse_diagnostic 重寫為四道閘門唯讀診斷（含 get_physicsui_instance 是否存在、timeline 狀態、
blockers 與處置建議）；LiveCarton 新增 mouse_no_shift()；carton_native_grab_test.py 與 scripts/pf-carton-native-grab 新增；
編輯器入口載入時印出完整診斷。未啟用擴充、未安裝、未重啟、未改摺痕參數或任何門檻。

命令與結果：
- `./scripts/pf-carton-native-grab` → exit 4 blocked，runs/20260917T134614Z_ext1_native_grab
- `./scripts/pf-carton-editor-test` → exit 0，runs/20260917T134525Z_ext1_editor_loader
- `python3 -m unittest discover -s tests` → 160 tests、7 skipped、exit 0
- `./scripts/pf smoke` → exit 0，runs/20260917T134634Z_s1_smoke

未驗證：原生滑鼠拖曳（需使用者 viewer 的 blockers 輸出）、真實紙板校正、render／影片／人眼。
下一個精確動作：使用者重跑 scripts/carton_force_editor.py 並貼出印出的 blockers 欄位。
文件：D063、STATE、EXT1 卡、reports/development/2026-09-17_mouse_gate_diagnosis.md。
