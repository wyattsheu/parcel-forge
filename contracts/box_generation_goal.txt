Generate a primitive rigid open-box asset for object-placement testing, not a deformable cardboard model. Use the frozen dimensions and probe requirements in the supplied parcel-forge case; do not change the task or acceptance profile. Represent the bottom and four sidewalls as separate semantic parts with a genuine open cavity. Keep SI metres and Z-up. Export render geometry and explicitly labelled collision candidates, with source revision, provider identity, units and SHA-256-bound artifacts. Do not fuse the cavity into a solid collision hull. Keep physical parameters with their provenance; estimates are not measurements. Do not claim acceptance: the trusted parcel-forge PhysX execution, trajectory readback and independent task gate decide placement success. Return inert source/artifacts through the configured provider contract, not commands that edit validators or evidence.
